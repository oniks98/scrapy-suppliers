# 🚀 Быстрый старт: Viatec.ua Spider'ы

## ✅ ШАГ 1: Установка зависимостей

```bash
cd C:\FullStack\Scrapy\suppliers
pip install -r requirements.txt
```

## 🔍 ШАГ 2: Тестирование селекторов (ОБЯЗАТЕЛЬНО!)

**⚠️ Перед запуском spider'ов нужно проверить селекторы:**

```bash
cd C:\FullStack\Scrapy\suppliers
python test_selectors.py
```

Этот скрипт покажет:
- Какие селекторы работают ✅
- Какие нужно заменить ❌
- Примеры найденных данных

**После теста обнови селекторы в файлах:**
- `suppliers/spiders/viatec_retail.py`
- `suppliers/spiders/viatec_dealer.py`

---

## 🛒 ШАГ 3: Запуск розничного spider'а (UAH)

```bash
scrapy crawl viatec_retail
```

**Что он делает:**
1. ✅ Загружает категории из `C:\FullStack\Scrapy\data\category_matching_viatec.csv`
2. ✅ Обходит все товары в категориях
3. ✅ Парсит розничные цены в UAH
4. ✅ Генерирует поисковые запросы (формат из образца)
5. ✅ Сохраняет в `C:\Users\stalk\Documents\Prom\prom_import.csv`

**Отладка:**
```bash
# Показать все логи
scrapy crawl viatec_retail -s LOG_LEVEL=DEBUG

# Ограничить 10 товарами (для теста)
scrapy crawl viatec_retail -s CLOSESPIDER_ITEMCOUNT=10

# Включить кэш (ускоряет тесты)
scrapy crawl viatec_retail -s HTTPCACHE_ENABLED=True
```

---

## 💰 ШАГ 4: Запуск дилерского spider'а (USD)

**Требует авторизации!** Проверь `.env`:
```
VIATEC_EMAIL=oniksdnipro@gmail.com
VIATEC_PASSWORD=Tvsat988
```

```bash
scrapy crawl viatec_dealer
```

**Что он делает:**
1. 🔐 Авторизуется на viatec.ua
2. ✅ Парсит дилерские цены в USD
3. ✅ Сохраняет в `C:\Users\stalk\Documents\Prom\prom_diler_import.csv`

**Отладка авторизации:**
```bash
scrapy crawl viatec_dealer -s LOG_LEVEL=DEBUG
```

Если авторизация не работает:
1. Открой `suppliers/spiders/viatec_dealer.py`
2. Проверь `login_url` (строка 21)
3. Проверь селекторы формы входа (метод `login`)

---

## 📊 ШАГ 5: Проверка результатов

### Файл розницы:
```
C:\Users\stalk\Documents\Prom\prom_import.csv
```

### Файл дилера:
```
C:\Users\stalk\Documents\Prom\prom_diler_import.csv
```

**Формат CSV:**
- Разделитель: `;` (точка с запятой)
- Кодировка: `UTF-8`
- Поля: согласно `product_sample_viatec.csv`

**Проверь:**
- ✅ Поисковые запросы в формате: `"Название, Слово1, Слово2, ..."`
- ✅ Категории из маппинга
- ✅ Цены очищены от лишних символов
- ✅ Валюта: UAH для розницы, USD для дилера
- ✅ До 30 характеристик товара

---

## ⚙️ Настройка селекторов

### Как найти правильные селекторы:

1. **Открой viatec.ua в Chrome**
2. **Нажми F12** (DevTools)
3. **Выбери инструмент "Inspect"** (Ctrl+Shift+C)
4. **Наведи на элемент** → видишь HTML

### Пример: найти селектор цены

```html
<!-- Если видишь такое: -->
<span class="product-price">1234 грн</span>

<!-- То селектор: -->
response.css("span.product-price::text").get()
```

### Где обновить селекторы:

**viatec_retail.py:**
- Строка 55: ссылки на товары
- Строка 77: название товара
- Строка 83: код товара
- Строка 87: цена
- Строка 95: описание
- Строка 99: изображения
- Строка 103: наличие
- Строка 107: производитель
- Строка 172: характеристики

**viatec_dealer.py:**
- Те же строки + строка 119: дилерская цена

---

## 🐛 Частые проблемы

### ❌ Ошибка: "No module named 'dotenv'"
```bash
pip install python-dotenv
```

### ❌ Не создаются CSV файлы
Проверь, что spider возвращает items с полем `price_type`:
```python
yield {
    "Назва_позиції": name,
    "Ціна": price,
    "price_type": "retail",  # или "dealer"
    ...
}
```

### ❌ Пустые CSV файлы
1. Запусти `test_selectors.py`
2. Обнови селекторы в spider'ах
3. Проверь, что категории в `category_matching_viatec.csv` актуальны

### ❌ Авторизация не работает (viatec_dealer)
1. Проверь учётные данные в `.env`
2. Открой viatec.ua вручную и залогинься
3. Посмотри в DevTools (Network → POST запрос при входе)
4. Обнови URL и параметры в `viatec_dealer.py`

---

## 📚 Полезные команды

```bash
# Показать все spider'ы
scrapy list

# Интерактивный shell для тестов
scrapy shell "https://viatec.ua/catalog/cameras/"

# В shell тестируй селекторы:
>>> response.css("h1::text").get()
>>> response.css("span.price::text").get()

# Запуск с кастомными настройками
scrapy crawl viatec_retail -s DOWNLOAD_DELAY=3 -s CONCURRENT_REQUESTS=1
```

---

## 📖 Документация

- **README_VIATEC.md** - подробная документация
- **test_selectors.py** - тестирование селекторов
- **settings.py** - настройки Scrapy
- **pipelines.py** - обработка данных

---

## ✅ Чек-лист перед запуском

- [ ] Установлены зависимости (`pip install -r requirements.txt`)
- [ ] Запущен `test_selectors.py`
- [ ] Обновлены селекторы в spider'ах
- [ ] Проверены учётные данные в `.env` (для dealer spider)
- [ ] Актуальны категории в `category_matching_viatec.csv`
- [ ] Существует директория `C:\Users\stalk\Documents\Prom\`

---

## 🎯 Что дальше?

После успешного парсинга:
1. Проверь CSV файлы в Excel/LibreOffice
2. Убедись в корректности данных
3. Загрузи на PROM.UA

Удачи! 🚀
