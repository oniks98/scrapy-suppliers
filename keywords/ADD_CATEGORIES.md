# Добавление новой категории

# Для создания сложной категории смотри шаблон C:\FullStack\Scrapy\keywords\processors\viatec\camera.py и также всю информацию обязательно ниже.

# Создание средней по сложности категории :

Полный процесс генерации ключевых слов состоит из **3 блоков**:

1. **Блок 1: Модель і бренд** - извлекается в `GenericProcessor._generate_model_keywords()`
2. **Блок 2: Характеристики** - извлекается через категорийный обработчик
3. **Блок 3: Універсальні фрази** - извлекается в `GenericProcessor._generate_universal_keywords()`

**Обработчик категории возвращает ТОЛЬКО Блок 2!**

---

## 📋 Последовательность действий

### 1. Добавить данные в CSV

`C:\FullStack\Scrapy\data\viatec\viatec_keywords.csv`

```csv
12345;https://url;universal_phrases_ru;universal_phrases_ua;base_keyword_ru;base_keyword_ua;allowed_specs
```

⚠️ **КРИТИЧНО:** `allowed_specs` - строгий список. Обработчик использует ТОЛЬКО эти характеристики.

---

### 2. Создать обработчик категории

`C:\FullStack\Scrapy\keywords\categories\viatec\{название}.py`

**Образец для копирования:** смотри `hdd.py`, `sd_card.py`, `usb_flash.py`

**Критичные правила:**

- Каждое `accessor.value()` СТРОГО через `is_spec_allowed()`
- Функция возвращает ТОЛЬКО характеристики (Блок 2)
- НЕ добавлять модель/бренд - это делает GenericProcessor
- НЕ добавлять universal phrases - это делает GenericProcessor

---

### 3. Зарегистрировать в роутере

`C:\FullStack\Scrapy\keywords\categories\viatec\router.py`

```python
from keywords.categories.viatec import hdd, новый_модуль

CATEGORY_HANDLERS = {
    "70704": hdd.generate,
    "12345": новый_модуль.generate,  # ← добавить
}
```

---

### 4. Экспортировать модуль

`C:\FullStack\Scrapy\keywords\categories\viatec\__init__.py`

```python
from keywords.categories.viatec import hdd, новый_модуль  # ← импорт

__all__ = ["hdd", "новый_модуль", "router"]  # ← добавить в список
```

---

### 5. Зарегистрировать в процессоре

`C:\FullStack\Scrapy\keywords\core\models.py`

```python
CATEGORY_PROCESSORS = {
    "301105": ProcessorType.CAMERA,
    "12345": ProcessorType.GENERIC,  # ← добавить
}
```

---

## ✅ Готово!

---

## 🔍 Проверка

```bash
python C:\FullStack\Scrapy\keywords\categories\viatec\check_compliance.py
```

Проверяет, что обработчик использует только `allowed_specs` из CSV.

---

## 📚 Образцы кода

### Простой обработчик

**Файл:** `keywords/categories/viatec/usb_flash.py`

### Средний обработчик

**Файл:** `keywords/categories/viatec/mounts.py`

### Сложный обработчик

**Файл:** `keywords/categories/viatec/hdd.py`

### Основной процессор

**Файл:** `keywords/processors/viatec/generic.py`

### Сложный процессор

**Файл:** `keywords/processors/viatec/camera.py`

---

## 🛠️ Полезные утилиты

```python
from keywords.utils.spec_helpers import (
    extract_capacity,      # Об'єм: "128gb"
    extract_interface,     # Інтерфейс: "usb 3.0", "sata"
    extract_speed,         # Швидкість: "90"
    extract_rpm,          # Обороти: "7200"
    is_spec_allowed       # Перевірка allowed_specs
)
```

**Примеры использования:** смотри в образцах кода выше.

---

## 📌 Чек-лист

- [ ] Категория добавлена в CSV с `allowed_specs`
- [ ] Обработчик создан (смотри образцы)
- [ ] Все `accessor.value()` через `is_spec_allowed()`
- [ ] Зарегистрирован в `router.py`
- [ ] Экспортирован в `__init__.py`
- [ ] Добавлен в `CATEGORY_PROCESSORS`
- [ ] Проверка `check_compliance.py` пройдена ✅
